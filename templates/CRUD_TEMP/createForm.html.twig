{% extends 'CRUD_TEMP/layout.html.twig' %}


{% block title %}CRUD Form
{% endblock %}


{% block body %}
<div>
<button type="button" class="btn btn-light btn-sm">Add More+</button>
</div>

{{ form_start(form) }}
    {{ form_widget(form) }} 

    <button class="btn btn-primary">{{ button_label|default('Register') }}</button>
{{ form_end(form) }}

<div class="row">


<div class="col-sm-12">

</div>
</div>

{% endblock %}