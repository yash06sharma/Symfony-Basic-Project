{% extends 'base.html.twig' %}

{% block title %}Testign{% endblock %}

{% block body %}
    <h1>Testign</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ testign.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ testign.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ testign.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_testign_index') }}">back to list</a>

    <a href="{{ path('app_testign_edit', {'id': testign.id}) }}">edit</a>

    {{ include('testign/_delete_form.html.twig') }}
{% endblock %}
