<form method="post" action="{{ path('app_testign_delete', {'id': testign.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ testign.id) }}">
    <button class="btn">Delete</button>
</form>
